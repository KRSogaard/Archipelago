version: 0.2

phases:
  install:
    runtime-versions:
        java: openjdk8
  build:
    commands:
      - ./gradlew kauai:build
  post_build:
    commands:
        - mv deploy/kauai/Procfile Kauai/build/libs/
artifacts:
    files:
        - 'Kauai.jar'
        - 'Procfile'
    base-directory: 'Kauai/build/libs/'